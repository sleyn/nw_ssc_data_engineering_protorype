# Demo-only secrets file, deliberately committed to git (ADR 0004).
# The values below are intentionally public (published demo credentials /
# a demo-only encryption key) so a reviewer can clone and run this repo with
# no manual secret-provisioning step. A real (non-demo) deployment would
# never commit this file.

# Fernet key used to encrypt/decrypt the runtime SQLite store at rest (ADR 0002).
encryption_key = "7KpTv_xQmca88GuQoIrylwgsebHp0YacQbzHd2wUTjY="
